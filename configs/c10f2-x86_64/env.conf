# Configuration for the c10f2 x86_64 gear-hsh-wrapper environment
# Used by the gear-hsh-wrapper-ctl.sh script invoked via a symlink (e.g., gear-hsh-wrapper-c10f2-x86_64)

# --- Required Variables ---
branch="c10f2"         # Target branch name
archTarget="x86_64"    # Target architecture

# --- Optional Overrides ---
# maintainerName="Your Name"              # Maintainer name for spec
# maintainerMail="your.email@example.com" # Maintainer email for spec
optSuffix=""  # Affects sandbox directory name within hasher (e.g., c10f2-x86_64-daily)
# argNoCheck="--no-p10-check=gpg,changelog" # Override default p10 checks
