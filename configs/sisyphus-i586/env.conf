# Configuration for the sisyphus i586 gear-hsh-wrapper environment
# Used by the gear-hsh-wrapper-ctl.sh script invoked via a symlink (e.g., gear-hsh-wrapper-sisyphus-i586)

# --- Required Variables ---
branch="sisyphus"      # Target branch name
archTarget="i586"      # Target architecture

# --- Optional Overrides ---
# maintainerName="Your Name"              # Maintainer name for spec
# maintainerMail="your.email@example.com" # Maintainer email for spec
optSuffix=""  # Affects sandbox directory name within hasher (e.g., sisyphus-i586-daily)
# argNoCheck="--no-sisyphus-check=gpg,changelog" # Override default sisyphus checks
